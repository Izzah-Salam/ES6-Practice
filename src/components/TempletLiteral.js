// Templet Literal
// It allow for easier and more readable string interpolation.They are enclosed by backticks (`) instead of single or double quotes
//

export default function TempletLiteral() {
  const name = "Izzah";
  const message = `Hello, ${name}!`;
  console.log(message); // Output: Hello, Izzah!
}
