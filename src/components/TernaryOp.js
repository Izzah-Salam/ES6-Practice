// It is a concise way to perform a conditional operation.
//It is often used as a shorthand for an if-else statement
// condition ? ifTrueExpression : IFfalseExpression

export default function TernaryOp() {
  let age = 18;
  console.log(age >= 18 ? "you caste the vote" : "you can not caste the vote");
}
